name: Generate Snake

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  generate:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Generate snake SVGs
        uses: Platane/snk@v3
        with:
          github_user_name: AriaDekami
          outputs: |
            dist/github-contribution-grid-snake-dark.svg?color_snake=%231F6FEB&color_dots=%2303002e,%23010048,%23010057,%2302006c,%23090088
            dist/github-contribution-grid-snake.svg?color_snake=%231F6FEB&color_dots=%2303002e,%23010048,%23010057,%2302006c,%23090088

      - name: Push to output branch
        uses: crazy-max/ghaction-github-pages@v4
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
